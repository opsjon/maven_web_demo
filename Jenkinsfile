pipeline {
  agent any
  
  options {
    timestamps()
  }
  
  parameters {
    string (name: 'version', defaultValue: '0.0.1', description: '版本')
  }
  
  stages{
    
    stage('getCode') {
      steps {
        println("Hello World !")
      }
    }
    
  }
}
